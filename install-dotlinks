#!/bin/sh

cd `dirname $0`

for dot in dot.*; do
  ln -vs .dotfiles/$dot ../.`echo $dot | sed 's/^dot.//'`
done

lesskey

for file in cshrc zshrc zshenv; do
  if [ ! -f ../.$file ]; then
    echo "source ~/.$file.base" > ../.$file
  fi
done
